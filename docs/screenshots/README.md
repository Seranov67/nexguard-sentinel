# Monitoring Screenshots

These images are captured from the local four-service Docker Compose stack with both
Prometheus scrape targets healthy. They contain no credentials or private endpoints.

- `grafana-dashboard.png` — provisioned NexGuard dashboard
- `prometheus-targets.png` — healthy Prometheus scrape targets
